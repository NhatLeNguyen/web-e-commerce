import DatabaseConnector from '../config/connectDB';
import bcrypt from 'bcryptjs';
import jwt from 'jsonwebtoken';
import dotenv from 'dotenv';

dotenv.config();

//đăng nhập trang chủ
let getHomePage = async (req, res) => {
  try {
    
  } catch (err) {}
};

export default {
  getHomePage,
};
